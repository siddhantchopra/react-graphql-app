# Demo app to use GrapghQL with Reactjs

## Available Scripts

In the project directory, you can run:

### Run `npm install` in root and /client

### Run `npm run dev` in root so that react and node app with get started concurrently

